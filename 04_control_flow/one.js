//if
if(2!=="2"){//true(either the datatype isn't eq or the value isn't eq)
    console.log("executed");
}

const temp=43
if(temp<40){
    console.log("<40");
}else{
    console.log(">40")
}

//scope of all the var inside if or else remains upto them only if we use let or const but in case of 'var' the scope is global so it'll be accessible globally 

//else if can be used to check multiple conditions 
